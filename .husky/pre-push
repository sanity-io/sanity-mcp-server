#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run only critical integration tests before pushing
# Skip TypeScript and Unit tests during push to avoid blocking development
npm run test:integration:critical
