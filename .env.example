# Sanity Configuration
# ------------------
# This file contains all environment variables required to run the Sanity MCP Server
# Copy this file to .env and fill in your own values

# Sanity API token with appropriate permissions
# Required for authentication and accessing Sanity APIs
# Create a token with read/write access at https://www.sanity.io/manage
SANITY_TOKEN=your_sanity_token_here

# Sanity project and dataset configuration
# These values are required for connecting to your Sanity project
SANITY_PROJECT_ID=your_project_id
SANITY_DATASET=production

# API version for Sanity (use latest stable)
# This determines which version of the Sanity API to use
SANITY_API_VERSION=2025-03-07

# Optional: OpenAI Configuration (if using embeddings features)
# ------------------
# Only required if you're using the semantic search/embeddings functionality
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Test Configuration
# ------------------
# These variables can be used to override defaults during testing
# TEST_PROJECT_ID=test_project_id
# TEST_DATASET=test_dataset
# TEST_TOKEN=test_token 